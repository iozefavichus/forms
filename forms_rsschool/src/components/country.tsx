import styles from '../styles/input.module.css';

const Country = () => {
    return (
        <div>
            Country:
            <input className={styles.root}/>
        </div>
    );
  };

export default Country;